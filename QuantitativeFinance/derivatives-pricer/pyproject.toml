[project]
name = "derivatives_pricer"
version = "0.1.0"
dependencies = [
    "numpy",
    "scipy",
    "fastapi",
    "uvicorn",
    "streamlit",
    "requests",
    "pytest"
]

[tool.setuptools.packages.find]
where = ["src"]